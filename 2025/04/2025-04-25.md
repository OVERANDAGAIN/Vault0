---
创建时间: 2025-四月-25日  星期五, 5:41:04 下午
---
```python
from ray.rllib.algorithms.alpha_zero import AlphaZeroTrainer
import torch

# 1. 恢复旧 trainer
old_trainer = AlphaZeroTrainer(config)
old_trainer.restore(r'C:\Users\admin\ray_results\AlphaZeroTrainer_StagHunt_2025-04-16_20-10-062ajps1ai\checkpoint_000251\checkpoint-251')

# 2. 获取旧 trainer 中 ToM1 策略的 weights
tom1_weights = old_trainer.get_policy("ToM1").get_weights()

# 3. 初始化新 trainer（注意不要 restore）
new_trainer = AlphaZeroTrainer(config)

# 4. 设置 ToM1 策略的 weights
new_trainer.get_policy("ToM1").set_weights(tom1_weights)
```
