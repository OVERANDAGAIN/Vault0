下面将 **你提供的整段方法部分（Section 3.1 + 部分 3.2）中出现的全部公式**
按照你需要的 **MathType（可直接复制到 PPT / Word）格式** 整理出来。

---

# 📘 **SMPE² 所有公式（MathType 可直接粘贴版）**

下面的公式均为 **独立块状公式**，每个都可直接拷贝进 MathType，无需修改。

---

# **📌（1）SMPE² 的整体优化目标（状态建模 + ELBO）**

[

]

---

# **📌（2）ELBO（变分下界）**

[
\mathrm{ELBO}(\omega_i, \phi_i;, o_i,; w_i\cdot o_{-i})
=======================================================

\mathbb{E}*{z_i \sim q*{\omega_i}}
\left[
\log p_{\phi_i}(w_i\cdot o_{-i} \mid z_i)
\right]
-------

\mathrm{KL}!\left(
q_{\omega_i}(z_i \mid o_i)
,\middle|,
p(z_i)
\right),
]

[
p(z_i)=\mathcal{N}(0,I)
]

---

# **📌（3）重构损失（Self-supervised Reconstruction Loss）**

[
L_{\mathrm{rec}}(\omega_i, \phi_i, \phi_i^{w})
==============================================

\left|
w_i^{e}\cdot o_{-i}
;-;
w_i \cdot \hat{o}_{-i}
\right|^2
\tag{3}
]

说明：

* (w_i^{e}) = filter used for reconstruction target
* (w_i) = AM filter learned for RL
* (\hat{o}*{-i} = p*{\phi_i}(z_i))

---

# **📌（4）Filter 稀疏正则（避免 w=0）**

[
L_{\mathrm{norm}}(\phi_i^{w})
=============================

*

| w_i|_2^{2}
\tag{4}
]

---

# **📌（5）KL 正则项（变分 encoder）**

[
L_{\mathrm{KL}}(\omega_i)
=========================

\mathrm{KL}!\left(
q_{\omega_i}(z_i \mid o_i)
,\middle|,
p(z_i)
\right),
\quad
p(z_i)=\mathcal{N}(0,I)
\tag{5}
]

---

# **📌（6）两个 Critic 的 TD 损失（central + filtered）**

### **Central Critic（MAA2C backbone）**

[
L_{\mathrm{critic}}(\xi)
========================

\left(
r_t^i + V^{\pi}*{\xi'}(s*{t+1})
-
V^{\pi}_{\xi}(s_t)
\right)^2
]

---

### **Filtered Critic（SMPE² 关键）**

[
L^{w}_{\mathrm{critic}}(\phi_i^{w},, k)
=======================================

\left(
r_t^i + V^{\pi}*{k'}(\hat{s}*{t+1})
-
V^{\pi}_{k}(\hat{s}_t)
\right)^2
\tag{6}
]

其中：

[
\hat{s}_t
=========

o_t^i
;\oplus;
\left(
w_i \cdot o_{-i}
\right)
]

---

# **📌（7）编码相关总损失（Encodings Loss）**

[
L_{\mathrm{encodings}}
======================

L^{w}*{\mathrm{critic}}
+
\lambda*{\mathrm{rec}}, L_{\mathrm{rec}}
+
\lambda_{\mathrm{norm}}, L_{\mathrm{norm}}
+
\lambda_{\mathrm{KL}}, L_{\mathrm{KL}}
\tag{7}
]

---

# **📌（8）Actor Loss（带 entropy + advantage）**

[
L_{\mathrm{actor}}(\psi_i)
==========================

-\beta_H \cdot
H!\left(
\pi_{\psi_i}(a_t^i \mid h_t^i, z_t^i)
\right)
]

[
\quad
-----

\log \pi_{\psi_i}(a_t^i \mid h_t^i, z_t^i)
\cdot
\left(
r_t^i
+
V^{\pi}*{\xi'}(s*{t+1})
-
V^{\pi}_{\xi}(s_t)
\right)
]

---

# **📌（9）最终 SMPE 总损失**

[
L_{\mathrm{SMPE}}
=================

L_{\mathrm{actor}}
+
L_{\mathrm{critic}}
+
L_{\mathrm{encodings}}
]

---

# 🎯 **附：如果需要 inline（行内）格式，也可额外提供。**

所有公式都已经是 MathType 可直接复制的规范 LaTeX 版本，不包含任何图片或格式污染，可无缝嵌入你的 PPT 文稿或 Word 附件。

---

# 📌 需要我进一步：

* 做 **公式索引表（第几节、第几页）**？
* 做 **方法模块结构图（loss 之间的数据流 + 梯度流）**？
* 做 **每个公式在 PPT 中的讲述用语**？

告诉我即可，我可以继续为你生成。
