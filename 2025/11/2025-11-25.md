论文： Enhancing Cooperative Multi-Agent Reinforement Learning with State modeling and Adversarial Exploration
[[Enhancing Cooperative Multi-Agent Reinforcement Learning with State Modelling and Adversarial Exploration]]


先把目前**已经知道的事实**捋一遍，然后我直接从你贴出的这两段代码里“找突破口”，给你一个比较硬的 debug 方案。

---

## 0. 目前实验现象（我们已经锁定的东西）

1. **环境侧：**

   * 4 × Random、4 × NS 的实验里，4 个 agent 的平均回报都差不多，没有稳定的
     $R_1 > R_2 > R_3 > R_4$ 这种序号偏置。
   * 说明：**环境本身 + VecEnv + logging 这条链路基本是对称的**。

2. **PPO 侧：**

   * “4 个 PPO 自博弈”时，平均 reward 最后都能到 2.8 左右，但：

     * player_1 的四个 loss 指标（entropy, pg, total, value）都比较正常、收敛平滑；
     * player_2/3/4 的 reward 虽然也收敛，但

       * entropy 一直在 0.5–0.7 附近乱跳甚至上升；
       * value_loss 频繁在几百甚至上千。
   * “3NS + 1PPO，并轮流把 PPO 绑成 player_1/2/3/4”：

     * 4 组的 **reward 都能收敛到 2.9 左右**；
     * 但 **只有当 PPO 是 player_1 时 loss 正常，其余三个 player_k 的 value_loss 都极大且震荡。**

=> 这一组对比已经非常明确地告诉我们：

> **环境几乎肯定没问题，真正有问题的是“PPO 训练管线里和 agent 索引相关的一段代码”。**
> 更具体一点：当 `my_id ≠ 1` 时，PPO 的 value head / advantage 计算方式和 `my_id = 1` 不一样（或者用了不属于自己的轨迹）。

---

## 1. 从环境代码出发：确认“环境没有结构性偏置”

你把 `StagHuntEnv` 贴出来其实是很好的一步，我们可以直接 check 里面所有和 `id` / `player_i` 相关的逻辑，看有没有结构性不对称。

### 1.1 观测重排 `__obs_state__`

```python
def __obs_state__(self, id):
    mystate = self.state.copy()
    mystate[:, :, :self.player_num - id] = self.state[:, :, id:self.player_num]
    mystate[:, :, self.player_num - id:self.player_num] = self.state[:, :, :id]
    return mystate
```

对 4 个玩家：

* id=0 → [0,1,2,3]
* id=1 → [1,2,3,0]
* id=2 → [2,3,0,1]
* id=3 → [3,0,1,2]

即：**每个 agent 看世界时，自己的 channel 始终在 index 0，其他人被循环平移。**
所以从观测结构上看，4 个位置是完全对称的。

### 1.2 奖励与终止

所有 reward / done 操作都在 `for id in avail_players_id` 的循环里，完全按 index 走，没有任何 “如果 id==0 就怎样” 的分支；
`player_strength` 默认 `[1,1,1,1]` 也没有破坏对称性。
`prosocial` 为 False 时那段奇怪的

```python
for i in ['player_1', 'player_2', 'player_4']:
```

不会被执行，所以现在也不影响。

综合来看：**你的环境在 4NS 和 random 实验中表现对称，本身没发现结构性的 agent_idx_bias。**
所以再继续往环境里抠 bug 的收益会很小。

---

## 2. 真正值得怀疑的链路：PPO 训练这条

现在重点就只剩一条路：**`HOPPlusPolicy.train()` 里面用了“错误的 agent 轨迹 / 错误的索引”，而这跟 `player_1` / `player_2/3/4` 恰好强相关。**

你贴出的主训练脚本里有几个关键点：

### 2.1 轨迹的结构

你在 rollout 结束后构造的是：

```python
Transition = namedtuple("Transition", [
    "obs_dict", "next_obs_dict", "reward", "done",
    "time_step", "env_id", "action", "log_prob", "value",
])

agent_trajectories = {aid: [] for aid in agent_ids}

# 每个 step 里
agent_trajectories[aid].append(
    Transition(
        obs_dict=obs_dict_raw,
        next_obs_dict=next_obs_raw,
        reward=torch.tensor(...),
        done=done_batch[aid][i],
        time_step=t,
        env_id=i,
        action=action_batch[i][aid],
        log_prob=item["logprob"],
        value=item["value"],
    )
)
```

这里本身没什么问题，并且 3NS+1PPO 的四组实验已经间接证明：**采样逻辑对 4 个位置是一样的**，不然 reward 本身就不会都收敛到 2.9。

### 2.2 训练调用方式

在 “单 PPO + 3NS” 的脚本里你是这样调用训练的：

```python
for aid in agent_ids:
    if aid != ppo_aid:
        continue
    policy_dict[aid].train(
        agent_trajectories=agent_trajectories,
        agent_ids=agent_ids,
    )
```

也就是说：

* **不管 PPO 是 player_1 / 2 / 3 / 4，`train()` 拿到的 `agent_trajectories` 字典结构都是一样的：**

  * `agent_trajectories["player_1"]`：NS 或 PPO 的轨迹；
  * `agent_trajectories["player_2"]`：NS 或 PPO 的轨迹；
  * ... 依此类推。
* 唯一变化的是：**哪一个 key 对应的轨迹是“我自己（PPO）”的轨迹，这依赖 `HOPPlusPolicy.train()` 里面的索引方式。**

> 换句话说：
> 👉 **如果 `HOPPlusPolicy` 内部始终默认用 `"player_1"` 那一条轨迹来算 loss，那么：**
>
> * 当 PPO 绑定在 player_1 上时，它刚好训练的是自己 → loss 正常；
> * 当 PPO 绑定在 player_2/3/4 上时，它仍然在用 `agent_trajectories["player_1"]` 的 reward / done / value 来算 loss，再拿这个 loss 回头更新“自己（player_k）”这套参数 →
>   reward 还能靠 self-play 慢慢混到 2.9，但 **value head 彻底失配，所以 value_loss/entropy 全乱了。**

这个“假设”其实已经被你那 4 组单 PPO 实验**强烈支持**了，而且它是**唯一同时解释：**

* 环境对称；
* 采样逻辑对称；
* reward 都能收敛；
* 但 “PPO 只在绑 player_1 时 loss 正常” 这件事。

接下来就不是“瞎猜”，而是 **设计最小的代码实验，直接证伪 / 证实这个假设。**

---

## 3. 具体的代码级 debug 步骤（建议一次性做完）

> 目标：确定 `HOPPlusPolicy` 在 `train()` 里到底用了哪一条轨迹。

### 3.1 在 `HOPPlusPolicy.__init__` 里加上一个明确的 self id

在 `policy_PPO.py` 里（HOPPlusPolicy 定义处），先找到构造函数，补两行：

```python
class HOPPlusPolicy:
    def __init__(self, config, env_creator, model_core, device="cuda"):
        self.config = config
        self.device = device
        ...
        # >>> 加上这两行，明确记录“我是谁”
        # 比如 make_hop_config 里本来就有 "agent_id" / "agent_name"
        self.my_aid = config.get("agent_id", None)
        self.my_idx = config.get("agent_idx", None)
        print("[HOPPlusPolicy init] my_aid =", self.my_aid, ", my_idx =", self.my_idx)
```

然后重新跑一次 3NS+1PPO（任意一个 ppo_player_id），确认：

* `player_1` 对应的 policy 实例打印出来的 `my_aid` / `my_idx` 是什么；
* 当你把 `ppo-player-id` 改成 2/3/4 时，这个 `my_aid` / `my_idx` 有没有变化。

**如果它始终是 `"player_1"` 或 `0`，那就直接抓到第一个大 bug 了。**

---

### 3.2 在 `HOPPlusPolicy.train()` 的开头，打印它实际用的轨迹 key

在 `HOPPlusPolicy.train()` 一开头加几行“暴力”的调试代码：

```python
def train(self, agent_trajectories, agent_ids, *args, **kwargs):
    # ---- Debug: 实际拿哪条轨迹在训练？----
    # 1) 打印这次函数调用时传进来的 key
    if hasattr(self, "_debug_printed") is False:
        print("[HOPPlusPolicy.train] my_aid =", self.my_aid,
              ", available keys:", list(agent_trajectories.keys()))
        self._debug_printed = True

    # 2) 你现在在代码中是怎么取出“自己的轨迹”的？
    #    假设原来类似这样：
    # my_trajs = agent_trajectories[self.my_aid]
    # 或者 agent_trajectories["player_1"] 之类
    # 请在这一行之前加上一个 assert：

    assert self.my_aid in agent_trajectories, \
        f"my_aid={self.my_aid} not in agent_trajectories keys {list(agent_trajectories.keys())}"
```

然后跑两组实验做对比：

* 只跑 100–200 个 update 就够了：

  * 一次 `--ppo-player-id=1`
  * 一次 `--ppo-player-id=2`

查看终端输出：

1. `my_aid` 是否随着 `ppo-player-id` 改变？
2. `my_trajs = agent_trajectories[???]` 这里用的 key 是什么？

如果你看到类似：

```text
[HOPPlusPolicy init] my_aid = player_1, my_idx = 0
[HOPPlusPolicy.train] my_aid = player_1, available keys: ['player_1', 'player_2', 'player_3', 'player_4']
```

而不管 `ppo-player-id` 设成几都是这样，那基本可以 99% 确认：

> **你的 PPO 始终在用 `player_1` 的轨迹做训练。**

这就是 agent_idx_bias 的根。

---

### 3.3 一旦确认问题，就给出 minimal 修复方案

假设上面的猜测被验证了，那么修复方式其实非常简单：

1. **保证 `make_hop_config` 为每个 agent 生成的 `cfg_by_agent[aid]` 里面，有正确的 `agent_id` 或 `my_aid` 字段。**

   比如在 `make_hop_config` 里：

   ```python
   for i, aid in enumerate(agent_ids):
       cfg = {...}
       cfg["agent_id"] = aid        # "player_1" / "player_2" ...
       cfg["agent_idx"] = i         # 0 / 1 / 2 / 3
       cfg_by_agent[aid] = cfg
   ```

2. **在 `HOPPlusPolicy.__init__` 用这个字段，而不要写死。**

   ```python
   self.my_aid = config["agent_id"]
   self.my_idx = config["agent_idx"]
   ```

3. **在 `train()` 里，取轨迹时统一用：**

   ```python
   my_trajs = agent_trajectories[self.my_aid]
   ```

   而不是：

   ```python
   my_trajs = agent_trajectories["player_1"]  # ❌
   my_trajs = agent_trajectories[agent_ids[0]]  # ❌
   ```

做完这一步之后，再跑一遍 3NS+1PPO 的 4 组实验，你期望看到的是：

* 四个 run 的 reward 曲线依然都能收敛到 2.8–2.9；
* 更关键：**四个 run 的 entropy / value_loss 形状都类似 player_1 那组，不再出现“player_1 正常，其他爆炸”的现象。**

---

## 4. 这一步之后的下一步

如果你按照上面的 debug 步骤，发现：

* `my_aid` 每次都设置正确；
* `train()` 也确实在用对应 key 的轨迹；

但仍然存在明显的 agent_idx_bias，那么下一步再往内收缩：

1. 在 `train()` 中，打印 / 用 TensorBoard 记录 **value target 与 predicted value 的分布**，按 agent 分开：

   * `mean(return_t)`, `std(return_t)`
   * `mean(value_t)`, `std(value_t)`
   * `corr(return_t, value_t)`

2. 看看：

   * player_1 的 value 是否在逐渐对齐 targets；
   * player_2/3/4 是否 value 一直在某个“奇怪的区间”里打转（比如全是 0 或全是某个常数）。

这一步就要真正打开 `HOPPlusPolicy` 里的 GAE / value_loss 计算部分了（可能需要你把那段代码再贴一次）。

---

## 小结（给你记笔记用的“阶段结论”）

你现在可以在你的 debug 笔记里这样写一小段：

---

你先按照 3.1 + 3.2 那两步把 `HOPPlusPolicy` 里的真实行为“曝光”出来，跑两组（ppo_player=1 和 2）看看 log；跑完把相关 print / 关键片段贴给我，我们再继续往里收缩。
