下面我根据你提供的 **Preliminaries → 2.2 State Modelling Optimization Framework** 部分，为你制作 **PPT 第 2 页（方法直觉 + SM 框架的理论背景支撑）** 的内容。

这页的作用很关键：

* 让听众理解：**本文的创新不是凭空出现的，而是从“状态建模（State Modelling）”理论框架出发的**
* 将“latent belief” 与 “policy optimization” 联系起来
* 引出后面的 SMPE² 方法（ELBO + AM Filters + Dual Critics）

我会做成 **更饱满、可讲 1.5 分钟的版本**。

---

# 🟦 **📍PPT 第 2 页：State Modelling Optimization Framework（理论基础）**

---

# 🎨 **PPT 内容（可直接粘贴）**

### **2. State Modelling Optimization Framework**

**Goal:**
Agents should infer **latent beliefs** ( z^i ) about the unobserved global state
**using only their own local observations**.

---

### **Why State Modelling?**

Real-world multi-agent states contain:

* **Redundant features**
  → irrelevant for agent (i)’s reward
* **Non-inferable features**
  → cannot be predicted from (o^i) due to partial observability

Therefore, agent (i) only needs a belief representation ( z^i ) that:

1. Contains **informative** features for policy optimization
2. Contains **only** features that are **predictable** from its own observation

---

### **Formal Definition**

Belief distribution:
[
z^i \sim p_{\omega_i}(z \mid o^i)
]

State Modelling objective:
[
V^*_{SM}

\max_{\omega, \psi}
;
V_{SM}(\omega,\psi)


\max_{\omega, \psi}
;
\mathbb{E}*{z \sim p*{\omega}}
\Big[
\mathbb{E}*{a \sim \pi*\psi,, s' \sim P,, o \sim F}
\sum_{t=0}^{\infty}
\gamma^t r_t
\Big]
\tag{1}
]

---

### **Key Theoretical Result**

**Proposition 2.1:**
[
V^*_{SM} = V^*
]

* The optimal value under the state-modelling formulation
  equals the optimal value of the original Dec-POMDP.
* Meaning:
  → Learning latent beliefs (z^i) **does not restrict** the optimal policy space
  → Belief-based policies can be **as good as** full-state optimal policies

---

### **Takeaway**

* SM forms the **theoretical foundation** of SMPE²
* It establishes the connection between:
  **representation learning ↔ policy optimization**
* Sets the stage for learning:

  * latent beliefs ( z^i )
  * filtered observations (AM)
  * value-aligned representations

---

# 📝 **Presenter Notes（口语稿，约 1.5 分钟）**

这一页我们介绍论文方法背后的理论基础：State Modelling Optimization Framework。

作者首先讨论了真实多智能体任务的一个重要事实：
全局 state 中往往包含大量冗余、噪声、对某个 agent 完全无关的特征；
更关键的是，**很多特征根本不可能从自身观察推断出来**，特别是在部分可观测环境中。

因此，本文提出一个关键假设：
Agent 不需要恢复原始 state；
它只需要学到一个 **对策略最重要、且可从自身观察中推断的 latent belief 表示**。

基于这个假设，作者定义了一个状态建模目标：
每个 agent 学一个条件分布
[
p_{\omega_i}(z^i | o^i)
]
让 latent belief (z^i) 能帮助 agent 实现最高的期望回报。

这在公式（1）中得到了形式化：
整体优化目标是对 encoder 参数 (\omega) 和 policy 参数 (\psi) 的联合最大化。

非常关键的是 Proposition 2.1：
它表明，在最优情况下，这个 state modelling 目标和原始 Dec-POMDP 的最优值是等价的。
换句话说，**引入 latent belief 不会降低可达的最优性能**，
反而提供了一种机制，让我们理解 agent 如何在部分可观测环境中形成 meaningful beliefs。

这为 SMPE² 提供了明确的理论支撑，也为后续的

* encoder–decoder
* AM filters
* dual-critic coupling
  奠定了基础。

---

# 🎯 为什么这一页很关键？

因为：

* 它让听众知道你不是在展示一个“工程型” trick，而是有理论支撑
* 它自然引出「为什么 SMPE² 要学习 latent belief」
* 它解释了“为什么不是重建全局状态，而是选择性建模”
* 它帮助听众理解 AM Filter 的动机（informative + inferable features）

---

如果你准备好了，我们继续：

👉 **PPT 第 3 页：SMPE² 整体结构图（Figure 1）完整解析版**
