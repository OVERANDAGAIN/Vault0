---
创建时间: 2026-二月-15日  星期日, 10:01:26 上午
---
```ad-tip
对，你这个理解和代码现状是吻合的：**原版 OMG_AM 是“一个算法同时服务 N 个 agent”的数据/计算布局**，所以它天然输出 `(…, N, D)` 的 subgoal；而你现在是 **每个进程/Policy 只控一个 agent**，并且 rollout 时只有 **my-obs**，因此除了 `subgoal_my` 以外的行在语义上基本都不成立，也不会被下游用到。

基于“事实 + 最小改动”，OMG 这里确实可以简化，而且简化点非常明确。
```

# 优化omg-am函数



