---
创建时间: 2026-五月-7日  星期四, 5:41:50 下午
---



## 一、Reward 模块与 Persona Mock User 系统完成整合

* 将原本独立测试的 RewardCalculator 接入 PersonaUserSimulator 主流程
* 保留 rollout 框架（agent.act → env.step → agent.observe实现, reward 在真实多轮交互中的在线计算
* 当前 reward 已可在完整 episode 中逐轮输出 breakdown（satisfaction / ad acceptance / annoyance / topic deviation...）



## 二、数据集接入

* 接入 user_profiles、initial_queries、topic_pool、ad_pool  ( 用户画像 , 初始 query,  topic 候选,广告池)

## 三、Reward 验证

* 正常对话下 reward 能维持较高用户价值
* 通过强制广告插入测试验证：

  * 不相关广告会显著降低 satisfaction
  * annoyance / overload / topic deviation 会明显上升
* 初步表明 reward 可以刻画 user value 与 business value 的 trade-off


## 四、当前发现的问题

* realism 加强，后期user planner 容易进入偏配合型、重复性较高的对话模式(后期更容易互相认同、礼貌性延续,完全同意 / 很有道理 / 谢谢你的建议)
* Planner 当前更偏用户价值，没有主动商业策略(插广告)
* 长对话存在 token 超限，需要历史裁剪回复长度控制


---
---

**Metrics 不能依赖我们自定义的 reward
如果 metrics 里还放 `total_reward / discounted_return / avg_s_t / avg_q_t`，那其实是在用方法内部定义去评价方法本身，不够独立。


>  metrics 的输入只来自 episode log、广告事件、用户行为事件和退出信息


# 一、Metrics 的原始输入

一个 episode 的日志可以抽象为：

$$\mathcal{E} = \{(y_t, u_t, ad_t, adID_t, click_t, done_t)\}_{t=1}^{T}$$
其中：

| 变量         | 含义          | 来源                        |
| - | -- | - |
| $T$        | 对话轮数        | episode log               |
| $y_t$      | 第 $t$ 轮系统回复 | agent log                 |
| $u_t$      | 第 $t$ 轮用户回复 | simulator / real user log |
| $ad_t$     | 是否插广告       | agent action              |
| $adID_t$   | 广告 ID       | agent metadata            |
| $click_t$  | 用户是否点击/接受广告 | simulator / user event    |
| $done_t$   | 是否退出        | environment               |
| $t_{exit}$ | 退出轮数        | episode log               |

这个定义是干净的，不依赖 reward。



# 二、设计独立 Metrics

## 1. Rule-based metrics

第一类是放能直接从 log 里算出来的。

这些指标不需要模型判断，也不依赖 reward。


### 1. 1对话留存类：Conversation Retention 

> 用户聊了多久？是否提前退出？

#### 1.1.1 Session Length

$$\text{SessionLength} = T$$

含义：一个 episode 中实际发生的对话轮数。

如果设最大轮数 $T_{\max}$，可以定义归一化留存：

$$\text{NormalizedRetention} = \frac{T}{T_{\max}}$$



#### 1.1.2 Early Exit

$$\text{EarlyExit} = \mathbb{I}(T < T_{\max})$$

多 episode 下：

$$\text{EarlyExitRate} = \frac{1}{N} \sum_{i=1}^{N} \mathbb{I}(T_i < T_{\max})$$

看是否提前结束。



#### 1.1.3 Exit Turn

$$t_{exit} = T$$

用于分析退出发生在对话前期、中期还是后期，辅助判断广告插入位置的长期影响。



### 1.2. 广告曝光与转化类：Ad Delivery / Conversion Metrics


#### 1.2.1 Revenue

每个广告有价值 $v(adID_t)$，则：

$$\text{Revenue} = \sum_{t=1}^{T} \mathbb{I}(click_t=1) \cdot v(adID_t)$$



#### 1.2.2 Revenue Density

$$\text{RevenueDensity} = \frac{\text{Revenue}}{T}$$

“单位轮次转化/单位轮次收益”。





## 2. Behavioral metrics
第二看用户行为结果, 比如：

* click / accept
* continue / stop
* correction
* complaint
* repeated clarification
* topic pull-back

这一层更接近用户真实反馈
>例如用户继续聊，可能是真的感兴趣，也可能只是还没完成任务；用户没点击广告，也不一定是不接受。

这些可以由：
* LLM judge
* BERT classifier
* human-labeled reward model
* 
估计。


### 2.1 用户显式摩擦：

这类不能用 reward 的 $q_t$，但可以基于用户文本里的**显式行为模式**。第一版可以规则，后续可以人工/外部 judge。

#### 2.1.1 Correction Count

用户把话题拉回原目标，例如：

* “我问的是……”
* “不是这个”
* “回到……”
* “I was asking about...”
* “Actually, I want...”

定义：

$$N_{corr} = \sum_{t=1}^{T} \mathbb{I}(u_t \text{ contains correction})$$


#### 2.1.2 Complaint Count

用户明确表达不满：

* “不要广告”
* “别推销”
* “无关”
* “annoying”
* “not related”

$$N_{complaint} = \sum_{t=1}^{T} \mathbb{I}(u_t \text{ contains complaint})$$



## 3. Human calibration

 如果有真人实验，latent states 可通过 survey 观测


真人实验中，可以通过打分近似观测 latent variables：

$$score \in \{1,2,3,4,5\}$$
如：
* 任务是否被解决？
* 广告是否自然？
* 是否愿意继续使用？
* 是否感到被打扰？



---

$$\text{Rule} \rightarrow \text{Behavioral} \rightarrow \text{Judge} \rightarrow \text{Human Calibration}$$
