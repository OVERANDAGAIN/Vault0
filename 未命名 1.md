---
创建时间: 2026-九月-4日  星期五, 8:43:45 晚上
---
# ComputeAstra 行业与竞品调研汇报演讲稿

大家好，我这次主要汇报一下最近做的 ComputeAstra 分布式 GPU 算力行业和竞品调研。整个调研其实不是为了回答哪家 GPU 最多、哪家价格最低，也不是想给这些产品做一个排名。实际体验下来，我觉得如果只用“GPU 算力平台”这一个标签去看，会把很多本质上完全不同的产品混在一起。所以这次我更想回答的是三个问题：第一，用户真正想完成的是什么；第二，平台到底替用户承担了哪些工作；第三，最后还有哪些复杂度仍然是用户自己承担的。

我们最开始很容易把这件事情理解成“用户缺一张 GPU，所以我们给他一张 GPU 就结束了”。但真正体验以后，我觉得这里至少要拆成几个不同的状态。第一层是资源已经分配并且开始计费；第二层是用户真的进入环境，并且能够确认 GPU 存在；第三层是业务代码真的运行了，而且结果已经生成并且被用户拿回来。这三个状态其实差别非常大。资源被分配，不代表用户已经能工作；环境能进去，不代表代码真的能跑；代码退出了，也不代表结果已经保存，更不代表这次事情真的结束了。

这次资源型产品里，这三个层级的差异非常明显。共绩这边，我实际恢复了一个 RTX 4090 的弹性部署，终端里能够确认 GPU、CPU 和内存，最后也完成了停止和账单，所以资源和环境是成立的，但本轮没有真正跑出业务结果。AutoDL 这边，我实际创建了 RTX 3080 Ti 实例，也产生了真实计费，但是没有真正进入可执行环境，所以它停在了“实例创建和计费”这个层面。矩池云走得最深，我实际创建了 A16，跑了 PyTorch 和 CUDA，故意制造了一次异常，也把结果文件拿了回来，最后释放实例。Vast.ai 则停在市场筛选和支付 Pending，没有真正进入实例。所以即使都叫“租 GPU”，用户实际上走到的终点完全不同。

基于这个现象，我觉得理解整个行业的第一步，不应该是先看品牌，而是先看这些产品到底在交付什么。有的产品交付的是一台可操作的 GPU 环境，有的是某个供应商的一条具体报价，有的是一次模型运行，有的是一个 Task，有的是构建好的镜像，还有一些产品甚至不想改变用户原来的开发方式，只是把执行位置从本地换到远端。所以这些产品并不是同一条产品路线上的不同成熟度，而是选择了不同的责任边界。

比如 GPU Cloud 和 GPU Marketplace，用户通常是从 GPU 型号、实例、镜像或者供应商报价开始，平台主要解决资源供给、创建、计量和基础入口，但环境、代码、数据、失败、结果和生命周期仍然有很多是用户自己负责。像 Replicate 这种 Model API，用户面对的不是一台机器，而是一次 Prediction，也就是一次模型运行。ClearML 更关注的是 Task，也就是一次有身份、有状态、有日志和结果关联的工作。SkyPilot 关注的是统一任务声明和底层基础设施适配。Railpack 和 Nixpacks 是直接从源码开始，解决 Source 到 Image 的问题。Docker Offload 则是在已有 Docker 工作流之上，试图只改变执行位置。

先说和我们最直接相关的资源型产品。共绩我这次看得比较深，因为它和普通的 GPU 实例平台不太一样。需求侧有弹性部署、Job、云主机、大模型云服务、裸金属、存储和镜像，同时供给侧还在做普通显卡和专业资源的接入。所以它不仅仅是在卖 GPU，它实际上还在搭一个双边市场。这个双边属性非常重要，因为它意味着长期价值不只取决于需求侧页面好不好用，还取决于供给审核、资源验证、调度、计量、结算和责任治理能不能真正形成闭环。

我在需求侧一开始其实想找的是一个适合 Python 和 AI 开发的云主机。但是当前账户进入云主机以后，页面是一个空状态，没有创建入口，也没有告诉我是库存、资格还是灰度问题。这个体验很典型，页面回答了“现在没有实例”，但没有回答“下一步怎么办”。所以第一次用户还要自己返回产品导航，重新判断是不是应该走 Job，或者改走弹性部署。

Job 是可以创建的，而且页面把抢占式 RTX 4090、显存、CPU、内存、小时价和秒价这些资源信息展示得很清楚。但是与此同时，用户也要理解镜像、启动命令、队列、并发、超时、重试这些概念。也就是说，共绩的信息透明度其实不错，但是产品还是以资源和调度作为主对象，而很多用户脑子里想的是“我有一段代码，帮我跑完，然后把结果给我”。这里就出现了一个比较明显的思维差异。

最后我选择恢复了一个已有的弹性部署。这个地方我觉得共绩做得挺好的一点，是“资源到底是不是真的”很好验证。页面上有节点状态、端口、区域、费用，进入终端以后又能直接看到 RTX 4090、18 核 CPU 和 63GB 内存。订单、节点、终端硬件和费用是能够互相印证的，而不是只显示一个抽象的“已开通”。这对于资源型平台来说其实很重要。

但从结果角度看，本轮弹性部署路径里没有观察到统一的结果目录、结果清单，或者停止之前提醒用户确认结果已经保存。这里我不想把它扩大成“共绩没有结果能力”，因为本轮没有把它所有产品都走一遍。更准确的说法是，在这次以业务结果为目标的弹性部署体验中，结果仍然主要是用户自己的责任。对实例型产品来说这不一定是缺陷，因为它本来交付的就是资源，但如果未来希望强调“帮用户把事情做完”，那这会是一个需要重新思考的责任边界。

停止和费用这块，共绩整体上处理得比较清楚。停止确认会明确告诉用户要释放全部节点、停止计费，而且重新启动的时候库存可能不保证。但是我实际操作的时候，出现过一个短暂状态：顶部已经显示“已停止”，节点下面还短暂显示“运行”，刷新以后才最终收敛。这个问题不是说它没有停，而是用户最关心“现在到底还在不在扣钱”的时候，不知道哪个状态是权威状态。最终账单是 0.8338 元，任务页和费用中心是一致的，所以最后状态是能追溯的。

供给侧我实际走到了审核。申请已经提交，个人身份和算力资源验证在进行中，账号激活还在等待。这里我觉得核心摩擦不是步骤多，而是审核过程缺少时间预期、补件、修改和沟通入口。供给方知道自己还在流程里，但不知道大概要等多久，也不知道如果有问题怎么处理。本轮没有走到设备接入、压力测试、定价、接单、收益和提现，所以这些部分我不会继续推断。

再看 AutoDL，它的产品逻辑就更加直接。AutoDL 已经替用户把操作系统、框架和 CUDA 组合成预置镜像，这一点对个人开发者很实用，因为不用从头搭环境。但用户仍然要理解显存、驱动和 CUDA 兼容、系统盘、数据盘、地区和具体宿主机库存。所以它适合的是已经比较清楚自己需要什么环境的人，而不是完全不懂资源的人。

这次 AutoDL 最有代表性的体验，是我第一次选择的主机在列表里显示 GPU 有空闲，但是创建时提示主机存储库存已经满了。第二次换了一台同价主机，又提示空闲 GPU 数量不足。第三次再换地区和宿主机才真正创建成功。这里其实错误提示本身并不差，它会明确告诉用户到底缺的是存储还是 GPU。真正的问题是，GPU 空闲只是创建条件之一，用户还是要自己反复拼接 GPU、存储、地区和具体宿主机的实时条件。所以“列表看起来有卡”并不等于“现在一定能创建”。

最终 AutoDL 创建了 RTX 3080 Ti，实际运行 20 分 53 秒，账单 0.40 元，最后关机。它做得比较好的地方是硬件和价格比较透明，镜像组合也清楚，而且关机以后实例、磁盘和释放逻辑会分开展示。本轮 JupyterLab 和 AutoPanel 在当前浏览器环境里没有成功打开，所以我没有把它写成“AutoDL 的 Jupyter 不可用”，只能说这次实际访问路径没有走通。整体来看，AutoDL 更像一台预装好环境的 GPU 开发机，而不是一个自动帮用户完成任务的平台。

矩池云的产品思路和 AutoDL 又有一点区别。它更接近一个标准化工作空间。用户先选 A16 这样的标准配置，再选镜像，不需要直接判断某一台宿主机，也不需要像 Vast.ai 那样判断供应商可靠性和网络差异。本轮我实际选择了 A16 15GB、3 个 CPU、14GB 内存、100GB 磁盘和 PyTorch 1.11 的镜像。

矩池这次走得比较完整。开始的时候余额是 0，0.1 元充值因为金额必须是整数而失败，1 元充值成功。但真正提交订单的时候才出现实名认证要求，完成支付宝认证以后才能继续租用。所以这里的问题不是实名认证本身，而是它出现得比较晚。用户已经选完卡、选完镜像、充完钱，才知道还有另外一道准入条件，这会产生比较明显的心理落差。

环境起来以后，本轮 Jupyter 外部链接在内置浏览器里没有正常打开，所以后来是通过同一个已经认证的 Jupyter 服务标准接口完成验证，没有绕过平台权限。实际环境里还遇到了一个镜像问题，默认的 python3 kernel 不存在，要切换到实际安装的 myconda。换完以后，PyTorch 能够识别 NVIDIA A16，CUDA 11.3 可用，矩阵计算也真实执行成功。我还主动抛了一个 RuntimeError，看到了 traceback，所以至少 Python 层面的失败对用户是可见的。

矩池在停止和释放这一步，我觉得做得比较完整。点击“停止并释放”的时候，它会明确告诉用户本地环境会丢失，如果只是短时间不用，可以选择暂停实例。确认以后，最终页面又会把停止成功、运行时长、标价费用、优惠抵扣和实付金额集中展示出来。这次运行 25 分钟，标价费用大约 0.22 元，被赠送的算力豆抵扣，实付是 0 元。这个体验的优点是，高影响操作之前解释后果，操作完成以后又把状态和费用重新收拢到一个终态页面里。

Vast.ai 的逻辑则更像真正意义上的 Marketplace。它没有试图把不同供应商的机器包装成完全一样的标准实例。用户会直接看到 GPU、显存、CPU、内存、磁盘速度、带宽、可靠性、验证状态、地区、CUDA、最长租期和价格。这里我觉得它做得很好的地方，是它承认供给本身就是异构的。对于懂基础设施的用户，这种透明度是有价值的。

但代价也很明显。普通 Python 用户在真正点击 Rent 之前，就要理解很多基础设施概念。模板可以帮用户解决“容器里装什么”，但它不会替用户回答“哪台主机适合我”。本轮我看到过 RTX 5060 Ti 16GB 的低价报价，但这只是当时的动态报价，不能当成固定目录价。本轮账户余额是 0，没有可用支付方式，两笔 10 美元的加款一直 Pending，所以实际体验停在支付层，没有真正租到实例。

把这几个资源型产品放在一起看，我觉得最重要的不是比较谁便宜，而是用户实际租到的对象完全不同。共绩是标准化节点和双边平台，AutoDL 是具体主机上的容器实例，矩池是标准工作空间，Vast.ai 是供应商的一条具体报价。它们各自都合理，但用户需要承担的判断不一样。

再往上看一层，如果用户根本不想管理实例，而是想直接拿到一次运行结果，那产品逻辑又不一样了。这里主要看 Replicate 和 RunPod。

Replicate 最典型的地方，是它围绕 Prediction，也就是一次模型运行来组织体验。调用者面对的是一个已经被别人包装好的 Model。输入参数、状态、输出、日志、JSON、时长、下载和近似费用都可以围绕同一次 Prediction 找到。对调用者来说，他甚至不需要知道背后具体是哪张 GPU。

本轮我实际选了一个官方模型，基本只填写 prompt，其他很多参数保留默认值，就能直接运行。最终生成了一张 720×720 的 WebP，页面显示大约 6.9 秒，结果和日志都可以真实下载。这个地方我觉得 Replicate 做得很好的是默认值。第一次用户不用先理解全部生成参数，也不用先理解 GPU，就可以先获得一次结果。之后如果要做 API 集成，同一套输入 schema 又可以切换到 JSON、Node.js、Python 和 HTTP 示例。

还有一个细节是输入校验。把必填 prompt 清空以后，页面会直接提示 This field is required，没有创建新的 Prediction，本轮也没有看到这次无效操作产生运行或费用记录。也就是说，它尽量把结构性的失败拦在真正创建运行对象之前。

但是调用者简单，不代表发布者也简单。我的实际体验只进入了 Create a model 和 Deployments，没有真的创建自己的 Model，也没有做 Docker 或 Cog。对于发布者来说，还是要准备代码、权重、依赖、预测入口、版本和硬件策略。所以 Replicate 的简单，本质上是复杂度被模型发布者、平台运行层和官方模型维护者吸收了，并不是复杂度消失了。

RunPod 则更加明显地把不同用户分流。它同时有 Pod、Serverless、Public Endpoints、Flash 等不同入口。这里我觉得它做得比较好的一个产品设计，是 Pod 和 Serverless 的对比页。它没有只列功能，而是用负载形状、持续容量和计费方式来解释什么时候该选 Pod，什么时候该选 Serverless。对第一次用户来说，这比直接给一堆专业名词更容易理解。

本轮我没有真正部署 Pod，也没有创建 Serverless Endpoint。实际走的是 Hello World 路径。页面会展示 handler(job)，也会告诉你同步、异步和查询状态的接口。但是继续往下以后，需要 GitHub repository。仓库 ready 以后，页面才提示账户至少要有 0.01 美元余额才能创建 Endpoint。账户当时余额是 0，充值页面能看到 Stripe 卡片和 Link，但没有可用国际卡，所以就停在这里。

这个体验里，错误本身其实很明确，它告诉我缺的是余额。真正的摩擦是这个条件出现得太晚，用户已经完成 GitHub 授权和仓库准备以后，才知道当前账户无法完成首跑。所以这不是 Serverless 本身技术能力的问题，而是 onboarding 前置条件出现时机的问题。

Replicate 和 RunPod 我不太建议简单比较谁更容易。因为本轮 Replicate 我扮演的是现成 Model 的调用者，而 RunPod 更接近一个自有 workload 发布者。两边起点不一样，复杂度也不可能一样。更准确的说法是，Replicate 把调用复杂度集中在 Model 和 Prediction 上，而 RunPod 给用户保留了 Pod、现成 API、Python 函数、GitHub 和 Docker 等多条入口。

再往后是 ClearML。ClearML 的思路和前面这些都不太一样，它的核心对象不是机器，而是 Task。Task、Queue、Agent、Worker、Artifact 分别承担不同职责。对于开发者来说，一开始这些概念会增加理解成本，但是一旦进入多人协作、重复运行和实验治理场景，这些对象就很有价值。

本轮我实际新建了项目和 Queue，用本地 Windows CPU Agent 注册了 Worker。一个非常有意思的实验是，脚本已经成功生成 result.txt，Task 也显示 Completed，但是 Artifacts 页面仍然显示 NO ARTIFACTS RECORDED。只有另外一条 Task 显式上传 JSON 后，Artifact 才真正出现，页面里可以看到路径、大小、哈希、预览和下载。

这个例子很好地说明了一件事情：Task 成功和结果被平台接管不是同一件事。文件存在于工作目录，不等于它已经成为一个可交接、可追踪的结果对象。

另外，没有 Worker 的时候，Task 会保持 Pending，Queue 里面能看到 Next Task，但是 Worker 数量是 0。我觉得这个状态语义很清楚。它没有把“资源没有到位”伪装成“代码失败”，用户不用从空日志里面猜任务到底有没有提交成功。

本轮还主动制造了 RuntimeError，失败 Task 会进入 Failed，Console 里保留 traceback、代码行和 exit code。之后我从失败 Task Clone 出一个新的 Task，修改以后重新执行成功。这里要注意，本轮验证的是 Clone，不是 Retry。整体来看，ClearML 很明显是面向 ML 工程团队的设计，它擅长把一次运行变成可以排队、追踪、复现和交接的工作对象，但第一次用户也需要理解更多平台实体。

SkyPilot 解决的是另一个问题：如果我已经有任务，但是底层基础设施可能变化，我不想每个云、每个 Kubernetes、每台 SSH 机器都重新学一遍怎么办。

SkyPilot 的产品定位是把资源、workdir、setup 和 run 放到统一任务声明里，然后去适配不同后端。不过本轮真实跑通的只有本地单节点 kind Kubernetes，而且是 CPU Task，没有实测外部云，也没有实测 GPU，所以这一点边界非常明确。

本轮实际链路是 Windows 和 WSL，到本地 Docker，再到 kind Kubernetes，再由 SkyPilot 执行一个 CPU Task，最后生成 result.txt，并通过 SSH 和 rsync 把结果取回来。

这次体验最有价值的地方，不是这个任务本身有多复杂，恰恰是任务非常简单，但仍然暴露出了真实基础设施复杂度。第一次 provision 的时候，kind 节点因为宿主机代理和容器网络命名空间的问题拉取镜像失败。也就是说，SkyPilot 可以给用户统一 YAML，但它不能让凭据、网络、镜像、驱动这些底层现实消失。

它做得好的地方，是失败阶段分得比较清楚。provision、setup 和用户 run 可以区分。本轮我主动制造了 setup exit 17 和 run exit 23，分别得到 FAILED_SETUP 和 FAILED。对技术用户来说，这种失败分层很有价值，因为至少知道应该修环境还是修自己的代码。

所以我的总体理解是，SkyPilot 的价值不是“帮我登录更多云”，而是让同一个计算任务在不同基础设施上拥有相似的声明、启动、连接、观察和回收方式。它最适合的是会写任务、需要外部算力、但不想成为每个基础设施专家的人。

Railpack 和 Nixpacks 又回到了运行之前的问题，也就是源码怎么变成一个可靠的运行环境。它们本质上都是 Source 到 OCI Image 的工具。Railpack 是 Nixpacks 的后继者，Nixpacks 已经进入维护模式。

这类工具值得看的点是，它们可以从源码识别语言、依赖、版本、入口、缓存，然后生成 build plan，再构建镜像。对于普通项目，这能够省掉很多重复 Dockerfile 工作。

但这里有一个非常关键的实验结果：Build success 不等于业务 success。比如 Flask 案例里，Railpack 生成了 gunicorn 入口，镜像运行以后 health 请求成功。Nixpacks 默认选择 python main.py，镜像构建成功，容器甚至以 0 退出，但实际上没有 HTTP 服务。加上 gunicorn override 以后，服务才恢复。

所以对于这类 Builder 来说，成熟体验不能停在“build 成功”，而应该继续到 Inspect Plan、Build、Run，再做 Business Check。否则很容易出现技术上全绿，但用户真正要的服务没有起来。

CUDA 也有类似问题。把 CUDA runtime 打进镜像，只能说明用户态依赖已经进入制品，并不能证明运行时已经真的拿到 GPU。GPU 是否可用还取决于运行时授权、宿主驱动和兼容条件。所以这里不能把“镜像构建成功”直接等同于“GPU 任务已经 ready”。

Docker Offload 的方向则更偏工作流连续性。它不是 Task 平台，也不应该拿 Task 或 Artifact 的标准来评价。它的目标是保留 Docker Desktop、Docker CLI、Dockerfile 和 Compose 的使用习惯，把实际 build 和 run 放到远端 Engine。

本轮没有真正启动远端 Offload，所以远端 build、run、GPU、端口、bind mount 和费用都没有实测。实际验证的是本地 Docker 基线，包括 run、build、cache 和错误都正常，然后看 Offload 的资格状态。

CLI 已经随 Docker Desktop 安装，但是诊断显示 DISABLED 和 UNAVAILABLE，账户没有组织、没有有效订阅，也没有可用 entitlement。登录后的产品页面里也没有很清楚的 Offload 自助购买、试用和价格入口。

所以它当前最大的摩擦并不是“用户不知道命令怎么用”，而是“命令已经在这里了，但是不知道怎么买、买什么、买完会得到什么”。

它真正值得我们关注的产品思想，是不要轻易改变用户已经熟悉的工作流。对于已有 Docker 用户来说，如果真的能把“换平台”压缩成“换执行位置”，这个体验会非常自然。但所谓工作流不变，只能理解成接口和操作习惯尽量不变，网络、缓存、文件、会话和成本这些远端语义仍然会变化。

把这些产品放在一起以后，我觉得有几个横向结论比较清楚。

第一个是，平台承担什么，用户承担什么，决定了产品是什么。资源型产品可以把 GPU 给用户，但是代码、结果和数据仍然可能是用户自己的责任。Replicate 会把一次运行组织成 Prediction。ClearML 会把一次工作组织成 Task。SkyPilot 会把任务在基础设施上的生命周期统一起来。Railpack 会把源码变成镜像。Docker Offload 则尽量保留原有 Docker 操作语言。

第二个是，“成功”到底由什么对象承载。对于资源平台，成功可能是一台机器 Running。对于 Replicate，是 Prediction Succeeded。对于 ClearML，是 Task Completed。对于 SkyPilot，是 cluster job 的阶段状态。对于 Builder，是 image build 成功。但这些成功不是同一回事，也不能直接排成成熟度阶梯。

第三个是，失败发生在哪里，决定了谁来处理。支付和准入阶段有矩池实名、Vast Pending、RunPod 余额、Docker Offload entitlement。库存阶段有 AutoDL 的 GPU 和存储。基础设施阶段有 SkyPilot 的镜像和网络。环境阶段有矩池 kernel。输入阶段有 Replicate prompt 校验。业务语义层有 Nixpacks 镜像构建成功但服务不可用。结果登记层有 ClearML 文件存在但没有 Artifact。

所以我觉得真正值得评价的，不是“一个平台有没有复杂度”，因为很多复杂度其实是必要的。更重要的是，这些复杂度是不是在正确的时间出现，用户能不能理解，以及平台有没有告诉用户下一步怎么办。

另外一个比较重要的点是结果。资源型产品通常给用户磁盘、终端或者工作空间，结果由用户自己决定放在哪里。Replicate 会把结果和一次 Prediction 绑定。ClearML 会把显式登记的结果变成 Artifact。SkyPilot 这次则是通过工作目录和 rsync 取回。Railpack 交付的是镜像，不是业务结果。Docker Offload 交付的是执行位置变化，也不是业务 Result。

所以结果管理是一个很重要的分界，但我不认为它应该成为评价所有产品的统一标准。对实例型产品来说，没有 Result 对象不一定是产品缺陷。但是如果我们的目标用户是“我只关心事情做完”，那结果保存、确认和交付就会变成很重要的产品责任。

生命周期和费用也是一样。Stop、Shutdown、Pause、Release、Destroy 这些动作不能当成一个意思。有的只停计算，有的会释放 GPU，有的保留磁盘，有的会销毁本地环境，有的甚至停止实例以后存储还继续收费。所以一个平台如果想让用户放心，至少要把三个问题讲清楚：现在什么资源还活着，数据还在不在，费用到底有没有停止。

最后回到 ComputeAstra。我觉得这次调研现在还不足以直接给出一个完整的产品架构，这也是我刻意没有在报告最后画一个“大而全架构图”的原因。因为在决定怎么做之前，我们首先要回答几个更基础的问题。

第一个问题是，我们真正要交付的对象是什么。到底是 GPU 节点，是一个可进入的开发工作空间，是一个可排队追踪的 Task，是一个 Endpoint，还是最终已经保存并能够取回的 Result。这个对象一旦不同，后面的产品承诺、界面、状态和指标都会完全不一样。

第二个问题是，我们能不能把资源已分配、环境内 GPU 已验证、业务代码和结果已完成这几个层级明确区分。现在很多产品容易把“机器 Running”当作核心状态，但用户真正想知道的是“我的事情现在做到哪一步了”。

第三个问题是，资源和结果应该有不同生命周期。环境可能是临时的，数据可能需要持久化，结果可能需要交付，镜像和快照又是另一类对象。这里如果不分清楚，用户很容易在停止资源的时候才发现结果还没有保存。

第四个问题是，把必要复杂度放到正确时间。像支付、实名、库存、Billing、entitlement 本身未必是不合理的，真正影响体验的是用户已经做了很多事情以后才知道自己不能继续。所以哪些条件应该前置检查，哪些可以延后，哪些失败应该在开始计费之前就被发现，这些都值得我们做产品验证。

第五个问题是，不能因为强调用户友好，就把所有专业控制都藏掉。GPU 型号、镜像、网络、端口、存储、版本这些信息，对专业用户都是真实需要。所以可能更合理的方向是渐进式呈现：第一次用户先看到目标、关键成本和下一步，专业用户再展开具体控制；默认值要帮助用户，而不是变成黑箱。

如果再结合我们双边平台的背景，供给侧还有一整套问题现在没有被验证完。包括个人和企业是不是应该走不同审核流程，供给者什么时候知道审核会结束，设备怎样接入，怎样解释没有任务，收益的不确定性怎样表达，供给质量和历史可靠性最后怎样传递给需求侧。这些其实都会决定双边平台能不能真正跑起来。

所以我最后的结论比较克制。不同产品并不在沿着同一条路径演进，它们是在用不同方式分配控制权、抽象程度和责任。我们真正值得学的，不是把所有产品的功能拼成一个大合集，而是看清楚每家已经把哪一段复杂度做得很好，再确定 ComputeAstra 到底准备替哪一类用户承担哪一段责任。

接下来我觉得最重要的不是继续画更大的架构，而是先确定一个最核心的用户和最核心的任务，然后跑一个真正完整的闭环。最后不要只看资源有没有启动，而是看用户到底有没有成功完成事情、拿到结果、理解失败，并且知道费用什么时候结束。如果这个闭环能跑通，再往后扩产品形态会更稳。
